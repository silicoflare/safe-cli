{
  "name": "safe-cli",
  "module": "safe-cli.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify safe.ts --outfile ./dist/safe"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.7.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^6.0.1",
    "chalk-template": "^1.1.0",
    "commander": "^12.1.0"
  }
}
