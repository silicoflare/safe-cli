{
  "name": "safe-cli",
  "module": "safe-cli.ts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build-exec": "bun build --compile --minify safe.ts --outfile ./dist/safe",
    "build": "bun build --minify safe.ts --outdir ./dist"
  },
  "bin": {
    "safe": "dist/safe.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.7.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^6.0.1",
    "chalk-template": "^1.1.0",
    "commander": "^12.1.0"
  }
}
